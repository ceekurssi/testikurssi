#include <stdio.h>
#include <math.h>
#include "source.h"

int my_sum_function(int a, int b) {
    /* i = 2 ja i == 2 */
    /* a1 & a2 ja a1 && a2 */
    /* b1 | b2 ja b1 || b2 */
    /* if (x = 1) ja if (x == 1) */
    /* while (condition) 
        statement 
            ja
       while (condition)
            ;
        statement */
    /* "a" ja 'a' */
    /* a < b < c ja a < b && b < c */
    
  
    
    return a + b;
}
