#include <stdio.h>
#include "source.h"

/* Muutetaan tätä tehtävää niin, että jos käyttäjän antama syöte on virheellinen, tulostetaan virheilmoitus stderr
 * Task is from Aalto C programming Basics Course
 * Objective: formatted input and output
 * Write function count_sum() that asks two integers from the user, calculates their sum and prints it in the following format: 
 * 1 + 2 = 3 
 * There should be a new line in the end. */

int main() {
    count_sum();
    return 0;
}
