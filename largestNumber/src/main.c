#include <stdio.h>
#include "source.h"

/* Write function maximum_integer that reads integers from user until user enters 0, incorrect integer or end-of-file.
 * After that function should return the maximum of given correct values.
 * Return 0, if user didn't enter any correct integer. */

int main(void) {
    printf("%d\n", maximum_integer());
    return 0;
}
