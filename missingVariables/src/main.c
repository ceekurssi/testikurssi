#include <stdio.h>
#include "source.h"

/* Objective: Learn to define variables
 * File source.c contains a function print_sums() that doesn't work. 
 * Define missing variables so that the function prints following lines:
 * 5 + 6 = 11
 * 2.3 + 4.1 = 6.4 */

int main() {
    print_sums();
    return 0;
}
