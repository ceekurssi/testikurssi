#include <stdio.h>
#include "source.h"

/* Implement function sum_of_many_numbers() that reads integers from the user until user gives 0 or non-integer. 
 * After that function should print sum of all correct given integers. */

int main()
{
    sum_of_many_numbers();
    return 0;
}
