#include <stdio.h>
#include "source.h"

/* Objective: Test that your development environment works and you can send tasks to the tmc-server.
 * Make sure that this task prints "Hello World!" 
 * and write to the function hello_world() 
 * a new printf statement that prints "and the universe." without the quotation marks. 
 * This line should also be followed by a new line. */

int main() {
    hello_world();
    return 0;
}
