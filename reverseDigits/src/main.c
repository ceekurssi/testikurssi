#include <stdio.h>
#include "source.h"

/* Write a function reverse_digits(int number), that takes an integer value and 
 * returns the number with its digits reversed.
 * For example if the number is 1234, the function should return 4321 */

int main() {
    printf("%d\n",reverse_digits(7345));
    return 0;
}
