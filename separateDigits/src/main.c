#include <stdio.h>
#include "source.h"

/* Write a function separate_digits(int number), that separates the number into its individual digits
 * and prints the digits separated from one another by a space.
 * For example if the number is 12345, the program should print: 
 * 1 2 3 4 5 */

int main() {
    separate_digits(1234509876);
    return 0;
}
